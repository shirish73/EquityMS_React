Step-by-Step Setup to Run the React Code:
1. Clone the repo to your local machine
2. cd EquityMS_React

# Install dependencies
npm install lucide-react and 
npm install

# Start the development server
npm start
